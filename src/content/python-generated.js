// Python — pre-generated pool for Generative mode.
//
// Shape: each chunk is { prompt, code } instead of a bare string.
// `prompt` is the English instruction shown to the learner; `code` is the
// hidden target they type from understanding, not from sight. See
// engine/practice-engine.js's codeOf/promptOf for how this is consumed,
// and ui/generative-view.js for how it's rendered (masked, ghost-dot).
//
// This file was expanded from a small starter set after real-usage
// feedback showed 1-snippet tiers made Skip/variety meaningless. Still
// smaller than the eventual target — extend by adding more { prompt,
// code } pairs to any tier's snippets array, or generate a larger batch
// elsewhere and paste it in following this exact shape.

export const tiers = [
  { name: "Warm-up", eyebrow: "Tier 1", required: 4, snippets: [
    { category: "core", chunks: [{ prompt: "Define a variable x and assign it the value 5.", code: "x = 5" }] },
    { category: "core", chunks: [{ prompt: "Assign the string \"Ada\" to a variable called name.", code: 'name = "Ada"' }] },
    { category: "core", chunks: [{ prompt: "Print the text hello world.", code: 'print("hello world")' }] },
    { category: "core", chunks: [{ prompt: "Assign the sum of a and b to a variable called total.", code: "total = a + b" }] },
    { category: "core", chunks: [{ prompt: "Set a boolean variable is_ready to True.", code: "is_ready = True" }] },
    { category: "core", chunks: [{ prompt: "Create a list called numbers containing 1, 2, and 3.", code: "numbers = [1, 2, 3]" }] },
    { category: "core", chunks: [{ prompt: "Create a dictionary called person with a key \"name\" set to \"Ada\".", code: 'person = {"name": "Ada"}' }] },
    { category: "core", chunks: [{ prompt: "Assign the length of items to a variable called length.", code: "length = len(items)" }] },
    { category: "core", chunks: [{ prompt: "Increment x by 1 using a compound assignment.", code: "x += 1" }] },
    { category: "core", chunks: [{ prompt: "Assign x squared to a variable called squared.", code: "squared = x ** 2" }] },
    { category: "core", chunks: [{ prompt: "Assign the price 19.99 to a variable called price.", code: "price = 19.99" }] },
    { category: "core", chunks: [{ prompt: "Create an empty list called items.", code: "items = []" }] },
    { category: "core", chunks: [{ prompt: "Set a boolean variable is_valid to False.", code: "is_valid = False" }] },
    { category: "core", chunks: [{ prompt: "Set a variable count to 0.", code: "count = 0" }] },
    { category: "core", chunks: [{ prompt: "Assign the string \"done\" to a variable called message.", code: 'message = "done"' }] },
    { category: "core", chunks: [{ prompt: "Create an empty dictionary called config.", code: "config = {}" }] },
    { category: "core", chunks: [{ prompt: "Create a tuple called point with the values 3 and 4.", code: "point = (3, 4)" }] },
    { category: "core", chunks: [{ prompt: "Assign None to a variable called result.", code: "result = None" }] },
    { category: "regex", chunks: [{ prompt: "Import the regular expressions module.", code: "import re" }] },
    { category: "strings", chunks: [{ prompt: "Split the variable line on commas into a list called parts.", code: 'parts = line.split(",")' }] },
    { category: "files", chunks: [{ prompt: "Import the os module.", code: "import os" }] },
    { category: "json", chunks: [{ prompt: "Import the json module.", code: "import json" }] },
    { category: "datetime", chunks: [{ prompt: "Import datetime and date from the datetime module.", code: "from datetime import datetime, date" }] },
  ]},
  { name: "Fluent", eyebrow: "Tier 2", required: 4, snippets: [
    { category: "core", chunks: [{ prompt: "Build a list of squares of numbers 0 through 9 using a list comprehension.", code: "squares = [n * n for n in range(10)]" }] },
    { category: "core", chunks: [{ prompt: "Build a list called evens containing only the even numbers from nums.", code: "evens = [n for n in nums if n % 2 == 0]" }] },
    { category: "core", chunks: [{ prompt: "Sum all values in the iterable values using a generator expression.", code: "total = sum(x for x in values)" }] },
    { category: "core", chunks: [{ prompt: "Build an f-string greeting that says Hello, followed by the name variable, then an exclamation mark.", code: 'greeting = f"Hello, {name}!"' }] },
    { category: "core", chunks: [{ prompt: "Assign the larger of a and b, compared by absolute value, to maximum.", code: "maximum = max(a, b, key=abs)" }] },
    { category: "core", chunks: [{ prompt: "Sort items in descending order into a new list called sorted_items.", code: "sorted_items = sorted(items, reverse=True)" }] },
    { category: "core", chunks: [{ prompt: "Double every number in nums using map and a lambda, collected into a list.", code: "doubled = list(map(lambda x: x * 2, nums))" }] },
    { category: "core", chunks: [{ prompt: "Assign \"ready\" to status if is_ready is true, otherwise \"waiting\", using a conditional expression.", code: 'status = "ready" if is_ready else "waiting"' }] },
    { category: "core", chunks: [{ prompt: "Unpack the first element of numbers into first and the rest into a list called rest.", code: "first, *rest = numbers" }] },
    { category: "core", chunks: [{ prompt: "Strip whitespace from value and lowercase it, assigning the result to clean.", code: "clean = value.strip().lower()" }] },
    { category: "core", chunks: [{ prompt: "Check whether s equals its own reverse and store the boolean in is_palindrome.", code: "is_palindrome = s == s[::-1]" }] },
    { category: "core", chunks: [{ prompt: "Check whether nums contains any duplicates by comparing set length to list length.", code: "has_dupes = len(set(nums)) != len(nums)" }] },
    { category: "core", chunks: [{ prompt: "Check whether a and b are anagrams by comparing their sorted characters.", code: "is_anagram = sorted(a) == sorted(b)" }] },
    { category: "core", chunks: [{ prompt: "Reverse the string s using slicing and assign it to reversed_str.", code: "reversed_str = s[::-1]" }] },
    { category: "core", chunks: [{ prompt: "Count the number of words in text by splitting on whitespace.", code: "word_count = len(text.split())" }] },
    { category: "core", chunks: [{ prompt: "Assign a list of unique items from the items list to unique_items.", code: "unique_items = list(set(items))" }] },
    { category: "core", chunks: [{ prompt: "Assign the intersection of setA and setB to common.", code: "common = setA & setB" }] },
    { category: "core", chunks: [{ prompt: "Find the longest word in words by length and assign it to longest.", code: "longest = max(words, key=len)" }] },
    { category: "core", chunks: [{ prompt: "Count how many items are truthy, assigning the count to count_true.", code: "count_true = sum(bool(x) for x in items)" }] },
    { category: "core", chunks: [{ prompt: "Convert a dictionary d's items into a list of tuples called entries.", code: "entries = list(d.items())" }] },
    { category: "core", chunks: [{ prompt: "Check whether any number in nums is negative, assigning the result to any_negative.", code: "any_negative = any(n < 0 for n in nums)" }] },
    { category: "core", chunks: [{ prompt: "Check whether every number in nums is positive, assigning the result to all_positive.", code: "all_positive = all(n > 0 for n in nums)" }] },
    { category: "regex", chunks: [{ prompt: "Search text for the first sequence of digits and assign the match object to match.", code: 'match = re.search(r"\\d+", text)' }] },
    { category: "regex", chunks: [{ prompt: "Find every sequence of digits in text as a list assigned to digits.", code: 'digits = re.findall(r"\\d+", text)' }] },
    { category: "regex", chunks: [{ prompt: "Replace runs of whitespace in text with a single space, assigning the result to cleaned.", code: 'cleaned = re.sub(r"\\s+", " ", text)' }] },
    { category: "strings", chunks: [{ prompt: "Lowercase text and split it into a list of tokens.", code: "tokens = text.lower().split()" }] },
    { category: "strings", chunks: [{ prompt: "Join the list parts with a hyphen separator into a variable called joined.", code: 'joined = "-".join(parts)' }] },
    { category: "strings", chunks: [{ prompt: "Pad the string form of n with leading zeros to 4 digits, assigning it to padded.", code: "padded = str(n).zfill(4)" }] },
    { category: "files", chunks: [{ prompt: "Check whether the path exists using the os module, assigning the result to exists.", code: "exists = os.path.exists(path)" }] },
    { category: "files", chunks: [{ prompt: "Join folder and filename into a single path called full_path.", code: "full_path = os.path.join(folder, filename)" }] },
    { category: "datetime", chunks: [{ prompt: "Assign the current datetime to a variable called now.", code: "now = datetime.now()" }] },
    { category: "datetime", chunks: [{ prompt: "Assign today's date to a variable called today.", code: "today = date.today()" }] },
    { category: "json", chunks: [{ prompt: "Parse the JSON string raw into a variable called data.", code: "data = json.loads(raw)" }] },
    { category: "json", chunks: [{ prompt: "Serialize data into a JSON string called text.", code: "text = json.dumps(data)" }] },
    { category: "validation", chunks: [{ prompt: "Assert that value is an instance of int.", code: "assert isinstance(value, int)" }] },
  ]},
  { name: "Compound", eyebrow: "Tier 3", required: 4, snippets: [
    { category: "core", chunks: [{ prompt: "Build a dictionary called pairs by zipping keys and values together.", code: "pairs = {k: v for k, v in zip(keys, values)}" }] },
    { category: "core", chunks: [{ prompt: "Filter data to only items where the \"active\" key is truthy, assigning the result to filtered.", code: 'filtered = [x for x in data if x.get("active")]' }] },
    { category: "core", chunks: [{ prompt: "Find the first item in items greater than 0, defaulting to None if none is found.", code: "result = next((x for x in items if x > 0), None)" }] },
    { category: "core", chunks: [{ prompt: "Merge base_config and overrides into a new dictionary called combined, with overrides taking priority.", code: "combined = {**base_config, **overrides}" }] },
    { category: "core", chunks: [{ prompt: "Sort a list of people by their name attribute, assigning the result to names.", code: "names = sorted(people, key=lambda p: p.name)" }] },
    { category: "core", chunks: [{ prompt: "Flatten a matrix (a list of lists) into a single list called flat.", code: "flat = [x for row in matrix for x in row]" }] },
    { category: "core", chunks: [{ prompt: "Strip each line in lines and keep only the non-empty ones, assigning the result to cleaned.", code: "cleaned = [s.strip() for s in lines if s.strip()]" }] },
    { category: "core", chunks: [{ prompt: "Transpose a matrix using zip and star-unpacking, assigning the result to transposed.", code: "transposed = list(zip(*matrix))" }] },
    { category: "core", chunks: [{ prompt: "Deduplicate items while preserving order using dict.fromkeys, assigning the result to deduped.", code: "deduped = list(dict.fromkeys(items))" }] },
    { category: "core", chunks: [{ prompt: "Build a dictionary mapping each word in text to its length, called word_lengths.", code: "word_lengths = {w: len(w) for w in text.split()}" }] },
    { category: "core", chunks: [{ prompt: "Safely divide a by b, assigning 0 to safe_div if b is zero.", code: "safe_div = a / b if b != 0 else 0" }] },
    { category: "core", chunks: [{ prompt: "Clamp value between 0 and 100, assigning the result to clamped.", code: "clamped = max(0, min(value, 100))" }] },
    { category: "core", chunks: [{ prompt: "Check whether the count of open and close parentheses in s match, assigning the boolean to is_balanced.", code: 'is_balanced = s.count("(") == s.count(")")' }] },
    { category: "core", chunks: [{ prompt: "Merge lists a and b into a single sorted list called merged.", code: "merged = sorted(list(a) + list(b))" }] },
    { category: "core", chunks: [{ prompt: "Build a dictionary mapping each value in items to its index, called index_map.", code: "index_map = {v: i for i, v in enumerate(items)}" }] },
    { category: "core", chunks: [{ prompt: "Get the top 3 keys from scores by value, sorted descending, into top_n.", code: "top_n = sorted(scores, key=scores.get, reverse=True)[:3]" }] },
    { category: "regex", chunks: [{ prompt: "Match phone in the form 3 digits, a dash, 4 digits, and extract the two groups.", code: 'groups = re.match(r"(\\d{3})-(\\d{4})", phone).groups()' }] },
    { category: "regex", chunks: [{ prompt: "Strip all non-alphanumeric characters from s, assigning the result to stripped.", code: 'stripped = re.sub(r"[^a-zA-Z0-9]", "", s)' }] },
    { category: "strings", chunks: [{ prompt: "Count character frequencies in text using collections.Counter, assigning the result to char_counts.", code: "char_counts = collections.Counter(text)" }] },
    { category: "files", chunks: [{ prompt: "Split filename into its name and extension using os.path.splitext.", code: "name, ext = os.path.splitext(filename)" }] },
    { category: "datetime", chunks: [{ prompt: "Compute the age in days between today and birth_date.", code: "age_days = (date.today() - birth_date).days" }] },
    { category: "json", chunks: [{ prompt: "Parse each line in lines as JSON, collecting the results into a list called parsed.", code: "parsed = [json.loads(line) for line in lines]" }] },
    { category: "validation", chunks: [{ prompt: "Check whether value is between 0 and 100 inclusive, assigning the boolean to is_valid_range.", code: "is_valid_range = 0 <= value <= 100" }] },
  ]},
  { name: "Composed", eyebrow: "Tier 4", required: 3, snippets: [
    { category: "core", chunks: [{ prompt: "Define a function add that takes a and b and returns their sum.", code: "def add(a, b):\n    return a + b" }] },
    { category: "core", chunks: [{ prompt: "Write a for loop that prints every item in items.", code: "for item in items:\n    print(item)" }] },
    { category: "core", chunks: [{ prompt: "Define a function is_even that returns whether n is divisible by 2.", code: "def is_even(n):\n    return n % 2 == 0" }] },
    { category: "core", chunks: [{ prompt: "Write a while loop that increments count until it reaches limit.", code: "while count < limit:\n    count += 1" }] },
    { category: "core", chunks: [{ prompt: "Sum all numbers in a list using a running total and a for loop.", code: "total = 0\nfor n in numbers:\n    total += n" }] },
    { category: "core", chunks: [{ prompt: "Open data.txt and read all its lines into a variable called lines.", code: 'with open("data.txt") as f:\n    lines = f.readlines()' }] },
    { category: "core", chunks: [{ prompt: "Define a function square that returns n multiplied by itself.", code: "def square(n):\n    return n * n" }] },
    { category: "core", chunks: [{ prompt: "Write a try/except that parses text as an int, defaulting value to 0 on failure.", code: "try:\n    value = int(text)\nexcept ValueError:\n    value = 0" }] },
    { category: "core", chunks: [{ prompt: "Define a recursive function factorial that returns n! (n factorial).", code: "def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n - 1)" }] },
    { category: "core", chunks: [{ prompt: "Define a Point class with an __init__ that sets self.x and self.y from arguments.", code: "class Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y" }] },
    { category: "core", chunks: [{ prompt: "Define a function is_prime that returns whether n has no divisors between 2 and n-1.", code: "def is_prime(n):\n    if n < 2:\n        return False\n    return all(n % i for i in range(2, n))" }] },
    { category: "core", chunks: [{ prompt: "Define a greet function with a default parameter name set to \"friend\", returning an f-string greeting.", code: 'def greet(name="friend"):\n    return f"Hi, {name}"' }] },
    { category: "core", chunks: [{ prompt: "Define a recursive Fibonacci function fib that returns the nth Fibonacci number.", code: "def fib(n):\n    if n <= 1:\n        return n\n    return fib(n - 1) + fib(n - 2)" }] },
    { category: "core", chunks: [{ prompt: "Define a function is_valid_email that checks whether s contains both @ and a period.", code: 'def is_valid_email(s):\n    return "@" in s and "." in s' }] },
    { category: "json", chunks: [{ prompt: "Define a function load_json that opens a file at path and returns its parsed JSON contents.", code: "def load_json(path):\n    with open(path) as f:\n        return json.load(f)" }] },
    { category: "validation", chunks: [{ prompt: "Define a function validate_age that raises a ValueError if age is negative.", code: 'def validate_age(age):\n    if age < 0:\n        raise ValueError("age must be non-negative")' }] },
  ]},
];
